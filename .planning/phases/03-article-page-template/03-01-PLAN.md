---
phase: 03-article-page-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils.ts
  - src/components/article/breadcrumbs.tsx
  - src/components/article/meta-line.tsx
  - src/components/article/key-takeaways.tsx
  - src/components/article/email-cta-placeholder.tsx
  - src/components/mdx/mdx-content.tsx
autonomous: true
requirements: [ART-03, ART-06, ART-07, ART-08]

must_haves:
  truths:
    - "Breadcrumbs render Home > Guides > Category Name > Article Title"
    - "Meta line shows published date, reading time, and category pill on all articles"
    - "Meta line conditionally shows updated date and verified badge only when updatedDate exists"
    - "Key takeaways box renders with burnt-orange left border and branded styling"
    - "Email CTA placeholder renders as a dashed-border placeholder block"
  artifacts:
    - path: "src/components/article/breadcrumbs.tsx"
      provides: "Breadcrumb navigation with category lookup"
      exports: ["Breadcrumbs"]
    - path: "src/components/article/meta-line.tsx"
      provides: "Article metadata line with conditional fields"
      exports: ["MetaLine"]
    - path: "src/components/article/key-takeaways.tsx"
      provides: "MDX custom component for key takeaways"
      exports: ["KeyTakeaways"]
    - path: "src/components/article/email-cta-placeholder.tsx"
      provides: "Static placeholder for future email form"
      exports: ["EmailCTAPlaceholder"]
    - path: "src/lib/utils.ts"
      provides: "formatDate and getTaxYear utility functions"
      contains: "formatDate"
  key_links:
    - from: "src/components/article/breadcrumbs.tsx"
      to: "src/lib/content/categories.ts"
      via: "getCategoryBySlug import"
      pattern: "getCategoryBySlug"
    - from: "src/components/article/meta-line.tsx"
      to: "src/lib/utils.ts"
      via: "formatDate and getTaxYear imports"
      pattern: "formatDate|getTaxYear"
    - from: "src/components/mdx/mdx-content.tsx"
      to: "src/components/article/key-takeaways.tsx"
      via: "sharedComponents map"
      pattern: "KeyTakeaways"
---

<objective>
Install dependencies (lucide-react, schema-dts) and build all server-side article sub-components: Breadcrumbs, MetaLine, KeyTakeaways, EmailCTAPlaceholder. Add date formatting utilities. Register KeyTakeaways in MDXContent's sharedComponents map.

Purpose: Create the foundational presentational components that the article page assembles. These are all server components with no client-side state.
Output: 6 files created/modified, 2 npm packages installed, all server components ready for page assembly in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-page-template/03-CONTEXT.md
@.planning/phases/03-article-page-template/03-RESEARCH.md
@.planning/phases/03-article-page-template/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/content/queries.ts:
```typescript
import { guides } from '#site/content';
export type Guide = (typeof guides)[number];
export function getGuideBySlug(slug: string): Guide | undefined;
export function getAllGuides(): Guide[];
export function getReadingTime(guide: Guide): number;
```

From src/lib/content/categories.ts:
```typescript
export interface CategoryConfig {
  slug: string;
  name: string;
  description: string;
  emoji: string;
}
export function getCategoryBySlug(slug: string): CategoryConfig | undefined;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/components/mdx/mdx-content.tsx:
```typescript
const sharedComponents = {
  // Custom MDX components will be added in Phase 3
};
export function MDXContent({ code, components }: MDXContentProps): JSX.Element;
```

Velite Guide shape (from velite.config.ts schema):
- title: string (max 70)
- description: string (max 160)
- slug: string
- publishDate: string (ISO date)
- updatedDate?: string (ISO date, optional)
- category: string (one of 8 category slugs)
- isPillar: boolean (default false)
- featured: boolean (default false)
- relatedSlugs: string[] (default [])
- keywords: string[] (default [])
- metadata: { wordCount: number, readingTime: number }
- toc: { title: string, url: string, items: TocEntry[] }[]
- body: string (compiled MDX code)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add date utility functions</name>
  <files>package.json, src/lib/utils.ts</files>
  <read_first>src/lib/utils.ts, package.json</read_first>
  <action>
1. Install lucide-react and schema-dts:
   ```bash
   npm install lucide-react schema-dts
   ```

2. Add two utility functions to `src/lib/utils.ts` (append after existing cn function):

   ```typescript
   /**
    * Format a date string to Irish-style "D MMM YYYY" format.
    * Example: "2026-03-15" -> "15 Mar 2026"
    */
   export function formatDate(dateString: string): string {
     const date = new Date(dateString);
     return date.toLocaleDateString('en-IE', {
       day: 'numeric',
       month: 'short',
       year: 'numeric',
     });
   }

   /**
    * Get Irish tax year string from a date.
    * Example: "2025-06-15" -> "2025/26"
    */
   export function getTaxYear(dateString: string): string {
     const year = new Date(dateString).getFullYear();
     return `${year}/${(year + 1).toString().slice(-2)}`;
   }
   ```

Per D-07: Tax year format is "YYYY/YY" derived from the updatedDate year.
Per D-09: Date format is "D MMM YYYY" (European format, day before month).
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && node -e "const { formatDate, getTaxYear } = require('./src/lib/utils.ts'); console.log('SKIP')" 2>/dev/null; npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/utils.ts contains `export function formatDate`
    - src/lib/utils.ts contains `export function getTaxYear`
    - formatDate uses `toLocaleDateString('en-IE', { day: 'numeric', month: 'short', year: 'numeric' })`
    - getTaxYear returns template literal `${year}/${(year + 1).toString().slice(-2)}`
    - package.json lists lucide-react and schema-dts in dependencies
    - `npx tsc --noEmit` passes with no type errors
  </acceptance_criteria>
  <done>Both utility functions exported from utils.ts, both npm packages installed, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Build Breadcrumbs, MetaLine, KeyTakeaways, EmailCTAPlaceholder, and update MDXContent</name>
  <files>src/components/article/breadcrumbs.tsx, src/components/article/meta-line.tsx, src/components/article/key-takeaways.tsx, src/components/article/email-cta-placeholder.tsx, src/components/mdx/mdx-content.tsx</files>
  <read_first>src/lib/utils.ts, src/lib/content/categories.ts, src/lib/content/queries.ts, src/components/mdx/mdx-content.tsx, src/app/globals.css, tailwind.config.ts, .planning/phases/03-article-page-template/03-UI-SPEC.md</read_first>
  <action>
Create `src/components/article/` directory, then build 4 server components and update MDXContent.

**1. breadcrumbs.tsx** (per D-08, ART-06)
Server component. Props: `{ categorySlug: string; categoryName: string; articleTitle: string }`.
- Render `<nav aria-label="Breadcrumb">` with `<ol>` containing 4 items: Home, Guides, Category, Article Title
- Home links to `/`, Guides links to `/guides`, Category links to `/guides/category/${categorySlug}` (per D-08, link works once Phase 4 builds category pages)
- Last item (article title) uses `aria-current="page"`, is NOT a link, truncated with `max-w-[200px] truncate` on mobile
- Separator: `<ChevronRight />` from lucide-react at `size={14}` with `className="text-stone-400"`
- All link text: `text-sm text-stone-600 hover:text-burnt-orange-500 transition-colors`
- Items separated by `gap-2`, overall flex wrap, `mb-4` bottom margin

**2. meta-line.tsx** (per D-07, D-08, D-09, ART-07, ART-08)
Server component. Props: `{ publishDate: string; updatedDate?: string; readingTime: number; categorySlug: string; categoryName: string }`.
- Horizontal flex row with items separated by middle dot `\u00B7` in `text-stone-400 mx-2`
- Published date: `<Calendar size={14} className="text-stone-500" />` + `formatDate(publishDate)` in `text-sm text-stone-600`
- Updated date (conditional, only when updatedDate truthy): "Updated " + `formatDate(updatedDate)` in `text-sm text-stone-600`
- Reading time: `<Clock size={14} className="text-stone-500" />` + `${readingTime} min read` in `text-sm text-stone-600`
- Category pill: `<Link href={/guides/category/${categorySlug}}>` with `text-sm font-bold px-2 py-1 rounded-full bg-burnt-orange-50 text-burnt-orange-600 hover:bg-burnt-orange-100 transition-colors`
- Verified badge (conditional, only when updatedDate truthy): `<ShieldCheck size={14} className="text-green-700" />` + `Verified for ${getTaxYear(updatedDate)} tax year` in `text-sm font-bold px-2 py-1 rounded-full bg-green-50 text-green-700`
- Items use `flex items-center gap-1` for icon-to-text spacing
- Outer container: `flex flex-wrap items-center gap-y-2 text-sm mb-4`

**3. key-takeaways.tsx** (per ART-03)
Server component. MDX custom component. Props: `{ children: React.ReactNode }`.
- Container: `bg-burnt-orange-50 border-l-4 border-burnt-orange-200 rounded-r-lg p-6 mb-12`
- Heading: `<h2 className="font-heading text-h3 text-charcoal mb-4">Key Takeaways</h2>` (uses text-h3 = 1.5rem since it's a sub-section heading within the article)
- Children rendered inside `<div className="space-y-2 text-stone-700 [&>ul]:list-disc [&>ul]:pl-5 [&>ul>li]:marker:text-burnt-orange-500">` to style the MDX bullet list
- No `"use client"` directive -- pure server component

**4. email-cta-placeholder.tsx** (per D-02)
Server component. No props.
- Container: `border-2 border-dashed border-stone-300 rounded-lg p-8 text-center mb-12`
- Heading: `<h2 className="font-heading text-h3 text-stone-500 mb-2">Stay Updated</h2>`
- Body: `<p className="text-sm text-stone-400">Email capture form coming soon.</p>`

**5. Update mdx-content.tsx**
- Import KeyTakeaways from `@/components/article/key-takeaways`
- Add to sharedComponents map: `{ KeyTakeaways }`
- NOTE: FAQSection will be added in Plan 02 when the FAQ client component is built

All components import `cn` from `@/lib/utils` where conditional classes are needed. Use `import Link from 'next/link'` for navigation links.
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npx tsc --noEmit --pretty 2>&1 | tail -5 && echo "--- File checks ---" && grep -l "export" src/components/article/breadcrumbs.tsx src/components/article/meta-line.tsx src/components/article/key-takeaways.tsx src/components/article/email-cta-placeholder.tsx 2>&1 && grep "KeyTakeaways" src/components/mdx/mdx-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/article/breadcrumbs.tsx exports `Breadcrumbs` with `aria-label="Breadcrumb"` and `aria-current="page"` on last item
    - src/components/article/meta-line.tsx exports `MetaLine` with conditional updatedDate and verified badge rendering
    - src/components/article/meta-line.tsx contains `getTaxYear` call and `formatDate` call
    - src/components/article/meta-line.tsx contains `bg-burnt-orange-50` for category pill and `bg-green-50` for verified badge
    - src/components/article/key-takeaways.tsx exports `KeyTakeaways` with `bg-burnt-orange-50` and `border-l-4 border-burnt-orange-200`
    - src/components/article/email-cta-placeholder.tsx exports `EmailCTAPlaceholder` with `border-dashed` and text "Stay Updated"
    - src/components/mdx/mdx-content.tsx imports KeyTakeaways and includes it in sharedComponents
    - No file contains `"use client"` directive
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 4 server components export correctly, MDXContent registers KeyTakeaways, TypeScript compiles, no client directives</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 4 new component files exist in src/components/article/
- MDXContent sharedComponents includes KeyTakeaways
- lucide-react and schema-dts in package.json dependencies
- No `"use client"` directive in any file created by this plan
</verification>

<success_criteria>
- Breadcrumbs renders Home > Guides > {Category} > {Title} with proper accessibility attributes (ART-06)
- MetaLine conditionally shows published date, updated date, reading time, category pill, verified badge (ART-07, ART-08, D-07, D-08, D-09)
- KeyTakeaways renders a branded box with burnt-orange accent (ART-03)
- EmailCTAPlaceholder renders a dashed placeholder (D-02)
- MDXContent maps KeyTakeaways for use in MDX articles
- formatDate and getTaxYear utilities are available for all components
</success_criteria>

<output>
After completion, create `.planning/phases/03-article-page-template/03-01-SUMMARY.md`
</output>
