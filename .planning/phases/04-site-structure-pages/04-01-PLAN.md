---
phase: 04-site-structure-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/article-card.tsx
  - src/components/shared/category-card.tsx
  - src/components/shared/disclaimer-bar.tsx
  - src/app/globals.css
  - src/app/page.tsx
autonomous: true
requirements: [PAGE-01, PAGE-10]

must_haves:
  truths:
    - "Homepage displays hero with Satoshi heading, value proposition, and browse CTA"
    - "Homepage shows 6 latest published articles as white cards on cream"
    - "Homepage shows 8 category cards in a 2x4 grid linking to category archives"
    - "Homepage shows EmailCTAPlaceholder section"
    - "Accounting disclaimer bar renders at the bottom of the homepage"
    - "ArticleCard shared component exists and is reusable across pages"
  artifacts:
    - path: "src/components/shared/article-card.tsx"
      provides: "Shared article card component"
      exports: ["ArticleCard"]
    - path: "src/components/shared/category-card.tsx"
      provides: "Category card with emoji and name"
      exports: ["CategoryCard"]
    - path: "src/components/shared/disclaimer-bar.tsx"
      provides: "Accounting disclaimer bar"
      exports: ["DisclaimerBar"]
    - path: "src/app/page.tsx"
      provides: "Homepage with hero, articles, categories, email CTA"
    - path: "src/app/globals.css"
      provides: "scrollbar-hide utility class"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/content/queries.ts"
      via: "getAllGuides() for latest 6 articles"
      pattern: "getAllGuides"
    - from: "src/app/page.tsx"
      to: "src/lib/content/categories.ts"
      via: "CATEGORIES for 8-card grid"
      pattern: "CATEGORIES"
    - from: "src/components/shared/article-card.tsx"
      to: "/guides/{slug}"
      via: "Link component"
      pattern: "href.*guides"
---

<objective>
Create shared UI components (ArticleCard, CategoryCard, DisclaimerBar) and build the homepage with hero section, latest articles grid, category grid, and email CTA placeholder.

Purpose: Shared components unlock the guides hub and category archives in Plan 02. The homepage is the site's front door and the primary landing page for organic traffic.
Output: 3 shared components, scrollbar-hide CSS utility, fully functional homepage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-site-structure-pages/04-UI-SPEC.md
@.planning/phases/04-site-structure-pages/04-CONTEXT.md
@.planning/phases/04-site-structure-pages/04-RESEARCH.md

@src/lib/content/queries.ts
@src/lib/content/categories.ts
@src/lib/utils.ts
@src/components/article/related-articles.tsx
@src/components/article/email-cta-placeholder.tsx
@src/app/page.tsx
@src/app/globals.css
@tailwind.config.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/content/queries.ts:
```typescript
export type Guide = (typeof guides)[number];
export function getAllGuides(): Guide[];
export function getReadingTime(guide: Guide): number;
```

From src/lib/content/categories.ts:
```typescript
export interface CategoryConfig {
  slug: string;
  name: string;
  description: string;
  emoji: string;
}
export const CATEGORIES: CategoryConfig[];
export function getCategoryBySlug(slug: string): CategoryConfig | undefined;
```

From src/components/article/related-articles.tsx (card pattern to extract):
```typescript
interface RelatedGuide {
  slug: string;
  title: string;
  description: string;
  category: string;
  categoryName: string;
  readingTime: number;
}
```

From src/components/article/email-cta-placeholder.tsx:
```typescript
export function EmailCTAPlaceholder(): JSX.Element;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components and scrollbar-hide utility</name>
  <files>
    src/components/shared/article-card.tsx
    src/components/shared/category-card.tsx
    src/components/shared/disclaimer-bar.tsx
    src/app/globals.css
  </files>
  <read_first>
    src/components/article/related-articles.tsx
    src/lib/content/categories.ts
    src/app/globals.css
    tailwind.config.ts
    .planning/phases/04-site-structure-pages/04-UI-SPEC.md
  </read_first>
  <action>
    **ArticleCard** (`src/components/shared/article-card.tsx`): Per D-05, extract the card pattern from RelatedArticles into a shared component. Server component (no "use client").

    Props interface:
    ```typescript
    interface ArticleCardProps {
      slug: string;
      title: string;
      description: string;
      category: string;
      categoryName: string;
      readingTime: number;
      isPillar?: boolean;
    }
    ```

    - Wrap entire card in `<Link href={/guides/${slug}}>` 
    - Container: `bg-white rounded-xl shadow-card hover:shadow-card-hover transition-shadow duration-200 overflow-hidden`
    - Inner padding: `p-6`
    - Category pill: `text-sm font-bold px-2 py-1 rounded-full bg-burnt-orange-50 text-burnt-orange-600`
    - If `isPillar`, render "Comprehensive Guide" badge inline after category pill: `text-sm font-bold px-2 py-1 rounded-full bg-burnt-orange-500 text-white ml-2`
    - Title: `font-heading text-body font-bold text-charcoal mt-3 mb-2`
    - Description: `text-sm text-stone-700 line-clamp-2`
    - Reading time: `text-sm text-stone-500 mt-3` showing "{readingTime} min read"

    **CategoryCard** (`src/components/shared/category-card.tsx`): Per D-02, create 2x4 grid cards. Server component.

    Props interface:
    ```typescript
    interface CategoryCardProps {
      slug: string;
      name: string;
      emoji: string;
    }
    ```

    - Wrap in `<Link href={/guides/category/${slug}}>` 
    - Container: `bg-white rounded-xl border border-stone-200 p-4 text-center hover:bg-burnt-orange-50 hover:border-burnt-orange-200 transition-colors duration-150`
    - Emoji: `text-2xl mb-2` block-level centered
    - Name: `font-heading text-sm font-bold text-charcoal`

    **DisclaimerBar** (`src/components/shared/disclaimer-bar.tsx`): Per D-13, create accounting disclaimer. Server component.

    - Container: `border-t border-stone-200 pt-6 mt-12`
    - Text: `text-sm text-stone-500 max-w-3xl`
    - Content: "The information on this site is for educational purposes only and does not constitute professional accounting, tax, or legal advice. For guidance specific to your situation, consult a qualified Irish accountant or tax adviser."

    **scrollbar-hide utility** in `src/app/globals.css`: Add inside `@layer utilities` block:
    ```css
    @layer utilities {
      .scrollbar-hide {
        -ms-overflow-style: none;
        scrollbar-width: none;
      }
      .scrollbar-hide::-webkit-scrollbar {
        display: none;
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/shared/article-card.tsx` exists and exports `ArticleCard`
    - `src/components/shared/category-card.tsx` exists and exports `CategoryCard`
    - `src/components/shared/disclaimer-bar.tsx` exists and exports `DisclaimerBar`
    - ArticleCard contains `shadow-card` and `hover:shadow-card-hover`
    - ArticleCard contains `isPillar` prop and "Comprehensive Guide" badge
    - CategoryCard contains `hover:bg-burnt-orange-50`
    - DisclaimerBar contains "educational purposes only"
    - globals.css contains `.scrollbar-hide` with `scrollbar-width: none`
    - No "use client" directive in any of the three components
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Three shared components export correctly, scrollbar-hide utility exists in globals.css, all type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build homepage (PAGE-01, PAGE-10)</name>
  <files>
    src/app/page.tsx
  </files>
  <read_first>
    src/components/shared/article-card.tsx
    src/components/shared/category-card.tsx
    src/components/shared/disclaimer-bar.tsx
    src/components/article/email-cta-placeholder.tsx
    src/lib/content/queries.ts
    src/lib/content/categories.ts
    src/app/page.tsx
    .planning/phases/04-site-structure-pages/04-UI-SPEC.md
  </read_first>
  <action>
    Replace the existing placeholder `src/app/page.tsx` with the full homepage. Server component (no "use client"). Per D-01, D-02, D-03, D-04.

    Imports:
    - `getAllGuides`, `getReadingTime` from `@/lib/content/queries`
    - `getCategoryBySlug`, `CATEGORIES` from `@/lib/content/categories`
    - `ArticleCard` from `@/components/shared/article-card`
    - `CategoryCard` from `@/components/shared/category-card`
    - `DisclaimerBar` from `@/components/shared/disclaimer-bar`
    - `EmailCTAPlaceholder` from `@/components/article/email-cta-placeholder`
    - `Link` from `next/link`
    - `ArrowRight` from `lucide-react`

    Data resolution (synchronous, at top of component):
    ```typescript
    const latestGuides = getAllGuides().slice(0, 6);
    ```

    **Section 1: Hero** (per D-01)
    - Container: `<section className="max-w-3xl mx-auto text-center py-18 px-6 md:px-8">`
    - Headline: `<h1 className="font-heading text-h1 md:text-[3rem] lg:text-[3.5rem] text-charcoal leading-[1.1]">The Ledger</h1>`
    - Value prop: `<p className="font-body text-body md:text-[1.25rem] text-stone-700 max-w-2xl mx-auto mt-6">Clear, jargon-free accounting guidance for Irish sole traders, company directors, and startup founders.</p>`
    - CTA: `<Link href="/guides" className="text-burnt-orange-500 hover:text-burnt-orange-600 font-body text-body font-bold inline-flex items-center gap-2 mt-8">Browse all guides <ArrowRight size={16} /></Link>`
    - Decorative rule: `<div className="h-px bg-stone-200 max-w-xs mx-auto mt-12" />`

    **Section 2: Latest Guides** (per D-03)
    - Container: `<section className="max-w-7xl mx-auto px-6 md:px-8 py-12">`
    - Heading: `<h2 className="font-heading text-h2 text-charcoal mb-8">Latest Guides</h2>`
    - Grid: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">`
    - Map `latestGuides` to `<ArticleCard>` components, computing `categoryName` from `getCategoryBySlug(g.category)?.name ?? g.category` and `readingTime` from `getReadingTime(g)` and `isPillar` from `g.isPillar`
    - Empty state: if `latestGuides.length === 0`, show: `<p className="text-base text-stone-500">Guides coming soon -- expert accounting content for Irish business owners.</p>`

    **Section 3: Browse by Topic** (per D-02)
    - Container: `<section className="max-w-7xl mx-auto px-6 md:px-8 py-12">`
    - Heading: `<h2 className="font-heading text-h2 text-charcoal mb-8">Browse by Topic</h2>`
    - Grid: `<div className="grid grid-cols-2 md:grid-cols-4 gap-4">`
    - Map `CATEGORIES` to `<CategoryCard slug={c.slug} name={c.name} emoji={c.emoji} />`

    **Section 4: Email CTA** (per D-04)
    - Container: `<section className="max-w-article mx-auto px-6 md:px-8 py-12">`
    - Render `<EmailCTAPlaceholder />`

    **Section 5: Disclaimer** (per D-13 / PAGE-10)
    - Container: `<div className="max-w-7xl mx-auto px-6 md:px-8 pb-12">`
    - Render `<DisclaimerBar />`

    Wrap everything in `<main className="min-h-screen bg-cream">`.
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` exports a default function component (not the old placeholder)
    - Contains "The Ledger" as H1 text
    - Contains "Browse all guides" CTA linking to `/guides`
    - Contains "Latest Guides" section heading
    - Contains "Browse by Topic" section heading
    - Contains `<ArticleCard` component usage
    - Contains `<CategoryCard` component usage
    - Contains `<EmailCTAPlaceholder` component usage
    - Contains `<DisclaimerBar` component usage
    - Uses `getAllGuides().slice(0, 6)` for latest articles
    - Uses `CATEGORIES` import for category grid
    - No "use client" directive
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Homepage renders hero, 6 latest article cards, 8 category cards, email CTA placeholder, and accounting disclaimer. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx tsc --noEmit` passes
- Homepage route `/` renders 5 sections: hero, latest guides, browse by topic, email CTA, disclaimer
- ArticleCard, CategoryCard, DisclaimerBar are importable from `@/components/shared/`
</verification>

<success_criteria>
- Homepage replaces the old placeholder with full editorial design
- 3 shared components exist and are reusable
- All 8 categories render in the grid
- Up to 6 latest published articles display as cards
- Accounting disclaimer renders at page bottom (PAGE-10)
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-site-structure-pages/04-01-SUMMARY.md`
</output>
