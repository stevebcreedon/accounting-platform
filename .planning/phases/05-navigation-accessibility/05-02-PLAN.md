---
phase: 05-navigation-accessibility
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/app/about/page.tsx
  - src/app/contact/page.tsx
  - src/app/find-accountant/page.tsx
  - src/app/guides/page.tsx
  - src/app/guides/category/[slug]/page.tsx
  - src/app/privacy/page.tsx
  - src/app/terms/page.tsx
  - src/app/not-found.tsx
autonomous: false
requirements: [NAV-03, PERF-05, PERF-06]

must_haves:
  truths:
    - "Every page renders with sticky header at top and footer at bottom"
    - "No page has nested <main> tags -- only the root layout <main> exists"
    - "No page renders a standalone DisclaimerBar -- disclaimer only appears in footer"
    - "Printing an article page produces a clean document without nav/footer/TOC"
    - "All pages render correctly at 375px with no horizontal overflow"
    - "Tab key navigates from skip-link through header through content through footer"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with SkipToContent, Header, main#main-content, Footer"
      contains: "main-content"
    - path: "src/app/globals.css"
      provides: "@media print styles for article pages"
      contains: "@media print"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "import Header"
      pattern: "import.*Header.*from.*layout/header"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "import Footer"
      pattern: "import.*Footer.*from.*layout/footer"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/skip-to-content.tsx"
      via: "import SkipToContent"
      pattern: "import.*SkipToContent.*from.*skip-to-content"
---

<objective>
Wire layout components into root layout, migrate all 9 page files (remove <main> wrappers and DisclaimerBar imports), add print styles, and verify responsive/accessibility behavior.

Purpose: After this plan, every page on the site renders inside the complete layout shell with header, footer, skip-to-content, and print support.
Output: Updated root layout, 9 migrated page files, print styles in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navigation-accessibility/05-CONTEXT.md
@.planning/phases/05-navigation-accessibility/05-RESEARCH.md
@.planning/phases/05-navigation-accessibility/05-UI-SPEC.md
@.planning/phases/05-navigation-accessibility/05-01-SUMMARY.md

@src/app/layout.tsx
@src/app/globals.css

<interfaces>
<!-- Layout components created in Plan 01 -->

From src/components/layout/skip-to-content.tsx:
```typescript
export function SkipToContent(): JSX.Element
// Renders <a href="#main-content">Skip to content</a>
```

From src/components/layout/header.tsx:
```typescript
export function Header(): JSX.Element
// Renders fixed header with "The Ledger" wordmark and Navigation
```

From src/components/layout/footer.tsx:
```typescript
export function Footer(): JSX.Element
// Renders 3-column footer with disclaimer and copyright
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root layout and migrate all 9 page files</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/about/page.tsx,
    src/app/contact/page.tsx,
    src/app/find-accountant/page.tsx,
    src/app/guides/page.tsx,
    src/app/guides/category/[slug]/page.tsx,
    src/app/privacy/page.tsx,
    src/app/terms/page.tsx,
    src/app/not-found.tsx
  </files>
  <read_first>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/about/page.tsx,
    src/app/contact/page.tsx,
    src/app/find-accountant/page.tsx,
    src/app/guides/page.tsx,
    src/app/guides/category/[slug]/page.tsx,
    src/app/privacy/page.tsx,
    src/app/terms/page.tsx,
    src/app/not-found.tsx,
    src/components/layout/header.tsx,
    src/components/layout/footer.tsx,
    src/components/layout/skip-to-content.tsx
  </read_first>
  <action>
    **Root layout update (src/app/layout.tsx):**
    - Add imports: `SkipToContent` from `@/components/layout/skip-to-content`, `Header` from `@/components/layout/header`, `Footer` from `@/components/layout/footer`
    - Update body content to:
      ```
      <SkipToContent />
      <Header />
      <main id="main-content" className="pt-16 min-h-screen">
        {children}
      </main>
      <Footer />
      ```
    - Keep all existing metadata, viewport, font loading unchanged

    **Page migration (all 9 files):**

    For EACH of these 9 files, make exactly 2 changes:

    1. **Remove DisclaimerBar import** -- delete the `import { DisclaimerBar } from '@/components/shared/disclaimer-bar';` line
    2. **Remove `<main>` wrapper** -- replace `<main className="min-h-screen bg-cream">` opening tag with a fragment `<>` and the closing `</main>` with `</>`. The `bg-cream` class is already on `<body>` in root layout. The `min-h-screen` is now on the root layout `<main>`.
    3. **Remove `<DisclaimerBar />`** JSX usage from the return statement

    Files to migrate (per D-05):
    - `src/app/page.tsx` -- remove DisclaimerBar import + usage, replace `<main>` with `<>`
    - `src/app/about/page.tsx` -- same
    - `src/app/contact/page.tsx` -- same
    - `src/app/find-accountant/page.tsx` -- same
    - `src/app/guides/page.tsx` -- same
    - `src/app/guides/category/[slug]/page.tsx` -- same
    - `src/app/privacy/page.tsx` -- same
    - `src/app/terms/page.tsx` -- same
    - `src/app/not-found.tsx` -- same

    CRITICAL: Do NOT modify `src/components/shared/disclaimer-bar.tsx` itself -- it stays in the codebase. Do NOT modify article page `src/app/guides/[slug]/page.tsx` -- it uses `<article>` not `<main>`.
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npx tsc --noEmit 2>&1 | head -30 && echo "---MAIN CHECK---" && grep -rl '<main' src/app/ 2>/dev/null | grep -v layout.tsx && echo "NESTED MAIN FOUND" || echo "NO NESTED MAIN - OK" && echo "---DISCLAIMER CHECK---" && grep -rl 'DisclaimerBar' src/app/ 2>/dev/null && echo "DISCLAIMER STILL IMPORTED" || echo "NO DISCLAIMER IMPORTS - OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import { SkipToContent }` and `import { Header }` and `import { Footer }`
    - `src/app/layout.tsx` contains `<main id="main-content" className="pt-16 min-h-screen">`
    - `src/app/layout.tsx` contains `<SkipToContent />` before `<Header />`
    - `src/app/layout.tsx` contains `<Footer />` after `</main>`
    - No file in `src/app/` (except layout.tsx) contains `<main` tag
    - No file in `src/app/` imports `DisclaimerBar`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Root layout renders SkipToContent, Header, main#main-content with pt-16, Footer. All 9 page files have no <main> wrappers and no DisclaimerBar imports. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add print styles and verify build</name>
  <files>
    src/app/globals.css
  </files>
  <read_first>
    src/app/globals.css,
    .planning/phases/05-navigation-accessibility/05-UI-SPEC.md
  </read_first>
  <action>
    Add `@media print` block at the END of `src/app/globals.css` (outside any @layer, per D-10):

    ```css
    @media print {
      @page {
        margin: 2cm;
      }

      body {
        background: white !important;
        color: black !important;
        font-family: Georgia, 'Times New Roman', serif !important;
        font-size: 12pt !important;
        line-height: 1.6 !important;
      }

      header,
      footer,
      nav,
      .sr-only {
        display: none !important;
      }

      main {
        padding-top: 0 !important;
      }

      h1 {
        font-size: 24pt !important;
        font-weight: bold !important;
        line-height: 1.3 !important;
      }

      h2 {
        font-size: 18pt !important;
        font-weight: bold !important;
        line-height: 1.3 !important;
      }

      h3 {
        font-size: 14pt !important;
        font-weight: bold !important;
        line-height: 1.3 !important;
      }

      .article-content a[href^="http"]::after {
        content: " (" attr(href) ")";
        font-size: 10pt;
        color: #666;
      }

      img {
        max-width: 100% !important;
        break-inside: avoid;
      }

      .break-inside-avoid {
        break-inside: avoid;
      }

      * {
        box-shadow: none !important;
      }
    }
    ```

    Then run `npm run build` to verify the full site builds with all layout changes.
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `@media print` block
    - Print styles include `@page { margin: 2cm; }` and `font-family: Georgia`
    - Print styles hide `header`, `footer`, `nav` with `display: none !important`
    - Print styles set `main { padding-top: 0 !important; }`
    - Print styles include `.article-content a[href^="http"]::after` for URL display
    - `npm run build` completes successfully with no errors
  </acceptance_criteria>
  <done>Print styles added to globals.css hiding navigation and applying serif typography. Full site builds successfully with all layout changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and accessibility verification</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete responsive layout shell across desktop, mobile, keyboard, and print.
    Run `npm run dev` and test all 10 verification points below.
  </action>
  <what-built>Complete responsive layout shell: sticky header with "The Ledger" wordmark and 3 nav links, full-screen mobile menu overlay, 3-column footer with disclaimer, skip-to-content link, print styles, and WCAG 2.1 AA compliance across all pages.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **Header:** Verify sticky header at top with "The Ledger" on left, Guides/Find an Accountant/About links on right
    3. **Active links:** Navigate to /guides -- "Guides" link should have burnt-orange bottom border. Navigate to /about -- "About" should be active.
    4. **Footer:** Scroll to bottom of any page -- verify 3-column layout (brand + tagline, EXPLORE links, LEGAL links), disclaimer text, and copyright
    5. **Mobile:** Resize to < 1024px width. Hamburger icon should appear. Click it -- full-screen cream overlay with large nav links. Click a link -- navigates and closes. Press Escape -- closes.
    6. **Skip link:** Press Tab on any page -- "Skip to content" badge should appear top-left in burnt-orange
    7. **Keyboard:** Tab through the page -- focus ring visible on all interactive elements
    8. **Print:** On an article page, press Cmd+P (or Ctrl+P) -- header, footer, and TOC should be hidden. Article text in serif font.
    9. **No duplicates:** Confirm NO page shows a standalone DisclaimerBar above the footer -- disclaimer only in footer
    10. **375px:** In DevTools, set viewport to 375px width -- no horizontal scrollbar on any page
  </how-to-verify>
  <verify>Human confirms all 10 verification points pass</verify>
  <done>User approves visual and accessibility behavior of complete layout shell</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- No nested `<main>` tags (only root layout has `<main>`)
- No `DisclaimerBar` imports in any page file
- `@media print` block exists in globals.css
- All ARIA labels present: "Main navigation", "Mobile navigation", "Footer navigation"
- Skip-to-content link targets `#main-content`
</verification>

<success_criteria>
- Every page renders with sticky header and footer from root layout
- Mobile menu overlay opens/closes correctly at < 1024px
- Print view of article pages hides nav/footer, shows serif text
- No horizontal overflow at 375px viewport
- Keyboard tab order: skip-link > header > content > footer
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/05-navigation-accessibility/05-02-SUMMARY.md`
</output>
