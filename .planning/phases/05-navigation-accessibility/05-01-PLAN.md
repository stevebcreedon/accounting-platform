---
phase: 05-navigation-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/skip-to-content.tsx
  - src/components/layout/header.tsx
  - src/components/layout/navigation.tsx
  - src/components/layout/mobile-menu.tsx
  - src/components/layout/footer.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-04]

must_haves:
  truths:
    - "Header component renders 'The Ledger' wordmark, 3 desktop nav links, and a hamburger button on mobile"
    - "Footer component renders 3-column grid with brand info, quick links, legal links, disclaimer bar, and copyright"
    - "Mobile menu overlay opens full-screen with focus trap, closes on Escape/X/link click"
    - "Skip-to-content link is visually hidden until focused, then appears as burnt-orange badge"
  artifacts:
    - path: "src/components/layout/skip-to-content.tsx"
      provides: "Skip-to-content accessibility link"
      contains: "Skip to content"
    - path: "src/components/layout/header.tsx"
      provides: "Sticky header with wordmark and navigation"
      contains: "The Ledger"
    - path: "src/components/layout/navigation.tsx"
      provides: "Client component with active link detection and hamburger"
      contains: "use client"
    - path: "src/components/layout/mobile-menu.tsx"
      provides: "Full-screen mobile overlay with focus trap"
      contains: "use client"
    - path: "src/components/layout/footer.tsx"
      provides: "3-column footer with disclaimer and copyright"
      contains: "DisclaimerBar"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/navigation.tsx"
      via: "import and render Navigation"
      pattern: "import.*Navigation.*from.*navigation"
    - from: "src/components/layout/navigation.tsx"
      to: "src/components/layout/mobile-menu.tsx"
      via: "import and render MobileMenu"
      pattern: "import.*MobileMenu.*from.*mobile-menu"
    - from: "src/components/layout/footer.tsx"
      to: "src/components/shared/disclaimer-bar.tsx"
      via: "import DisclaimerBar"
      pattern: "import.*DisclaimerBar.*from.*disclaimer-bar"
---

<objective>
Create all 5 layout components: skip-to-content link, sticky header with wordmark and navigation, mobile menu overlay with focus trap, and 3-column footer with integrated disclaimer bar.

Purpose: These components form the complete navigation chrome that wraps every page. They must exist before Plan 02 wires them into the root layout and migrates pages.
Output: 5 new component files in src/components/layout/ ready for root layout integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navigation-accessibility/05-CONTEXT.md
@.planning/phases/05-navigation-accessibility/05-RESEARCH.md
@.planning/phases/05-navigation-accessibility/05-UI-SPEC.md

@src/app/layout.tsx
@src/app/globals.css
@src/components/shared/disclaimer-bar.tsx
@src/lib/utils.ts

<interfaces>
<!-- Existing utilities and components the executor needs -->

From src/lib/utils.ts:
```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

From src/components/shared/disclaimer-bar.tsx:
```typescript
export function DisclaimerBar() {
  // renders disclaimer text in border-t div
}
```

Tailwind config tokens (from Phase 1):
- `font-heading` = Satoshi (--font-satoshi)
- `font-body` = DM Sans (--font-dm-sans)
- `text-h2` = 2rem, `text-h3` = 1.5rem
- `cream` = #FAF8F5, `charcoal` = #1C1917
- `burnt-orange-500` = #E8720C
- `stone-200`, `stone-500`, `stone-600`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skip-to-content, header, and navigation components</name>
  <files>
    src/components/layout/skip-to-content.tsx,
    src/components/layout/header.tsx,
    src/components/layout/navigation.tsx
  </files>
  <read_first>
    src/app/layout.tsx,
    src/app/globals.css,
    src/lib/utils.ts,
    .planning/phases/05-navigation-accessibility/05-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/layout/` directory and 3 files:

    **skip-to-content.tsx** (server component, per D-08):
    - Export `SkipToContent` function component
    - Render `<a href="#main-content">Skip to content</a>`
    - Classes: `sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[60] focus:bg-burnt-orange-500 focus:text-white focus:px-4 focus:py-2 focus:rounded-lg focus:text-sm focus:font-bold focus:shadow-lg`

    **header.tsx** (server component, per D-01, D-03):
    - Export `Header` function component
    - Render `<header className="fixed top-0 left-0 right-0 z-50 h-16 bg-cream/95 backdrop-blur-sm border-b border-stone-200">`
    - Inner container: `<div className="max-w-7xl mx-auto px-4 md:px-8 h-full flex items-center justify-between">`
    - Logo: `<Link href="/" className="font-heading text-h3 font-bold text-charcoal">The Ledger</Link>`
    - Import and render `<Navigation />` from `./navigation`
    - Add `<nav aria-label="Main navigation">` wrapping the desktop links inside Navigation

    **navigation.tsx** ("use client" component, per D-02, D-07):
    - `'use client'` at top
    - Import `usePathname` from `next/navigation`, `Link` from `next/link`, `Menu` from `lucide-react`, `cn` from `@/lib/utils`
    - Import `MobileMenu` from `./mobile-menu`
    - Define `NAV_LINKS` array:
      ```
      { label: 'Guides', href: '/guides', match: (p: string) => p.startsWith('/guides') }
      { label: 'Find an Accountant', href: '/find-accountant', match: (p: string) => p === '/find-accountant' }
      { label: 'About', href: '/about', match: (p: string) => p === '/about' }
      ```
    - Desktop nav: `<nav aria-label="Main navigation" className="hidden lg:flex items-center gap-8">`
    - Each link: `<Link>` with `cn('text-sm font-bold transition-colors duration-150 py-1', isActive ? 'text-charcoal border-b-2 border-burnt-orange-500' : 'text-stone-600 hover:text-burnt-orange-500')`
    - Hamburger button: `<button className="lg:hidden flex items-center justify-center w-11 h-11 -mr-2" aria-label="Open menu" aria-expanded={isMenuOpen}>`
    - Render `<Menu className="h-6 w-6 text-charcoal" />`
    - `useState` for `isMenuOpen` boolean
    - Pass `isOpen`, `onClose`, and `pathname` to `<MobileMenu />`
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/layout/skip-to-content.tsx` exists and contains `Skip to content` and `#main-content`
    - File `src/components/layout/header.tsx` exists and contains `The Ledger` and `fixed top-0` and `z-50` and `h-16`
    - File `src/components/layout/navigation.tsx` exists and contains `use client` and `usePathname` and `Main navigation` and `aria-expanded`
    - Header imports Navigation from `./navigation`
    - Navigation imports MobileMenu from `./mobile-menu`
    - Desktop nav links are hidden on mobile: `hidden lg:flex`
    - Hamburger button visible only on mobile: `lg:hidden`
  </acceptance_criteria>
  <done>Three layout components exist: skip-to-content link targets #main-content, header is fixed with z-50 and renders "The Ledger" wordmark, navigation has active link detection with usePathname and hamburger button with aria-expanded</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile menu overlay and footer components</name>
  <files>
    src/components/layout/mobile-menu.tsx,
    src/components/layout/footer.tsx
  </files>
  <read_first>
    src/components/layout/navigation.tsx,
    src/components/shared/disclaimer-bar.tsx,
    .planning/phases/05-navigation-accessibility/05-UI-SPEC.md
  </read_first>
  <action>
    **mobile-menu.tsx** ("use client" component, per D-06):
    - `'use client'` at top
    - Import `useEffect, useRef, useCallback` from react, `Link` from `next/link`, `X` from `lucide-react`
    - Props interface: `{ isOpen: boolean; onClose: () => void; pathname: string }`
    - Container: `<div role="dialog" aria-modal="true" aria-label="Site navigation" className="fixed inset-0 z-50 bg-cream flex flex-col items-center justify-center">`
    - CSS transition: wrap in a conditional render. When `isOpen` is false, do not render. Apply `transition-opacity duration-150` with opacity states.
    - Close button: `<button className="absolute top-4 right-4 flex items-center justify-center w-11 h-11" aria-label="Close menu" onClick={onClose}>` with `<X className="h-6 w-6 text-charcoal" />`
    - Wordmark in overlay: `<span className="font-heading text-h2 font-bold text-charcoal mb-12">The Ledger</span>`
    - Nav links container: `<nav aria-label="Mobile navigation" className="flex flex-col items-center gap-6">`
    - Primary links (Guides, Find an Accountant, About): `<Link className={cn('font-heading text-h2 font-bold', pathname matches ? 'text-burnt-orange-500' : 'text-charcoal')} onClick={onClose}>`
    - Secondary contact link: `<Link href="/contact" className="font-body text-base text-stone-600 mt-8" onClick={onClose}>Contact</Link>`
    - Body scroll lock: `useEffect` that sets `document.body.style.overflow = 'hidden'` when open, cleans up with `document.body.style.overflow = ''`
    - Focus trap: `useEffect` querying focusable elements in the overlay `overlayRef`, cycling Tab between first and last, handling Shift+Tab reverse. Close on Escape key.
    - Auto-focus close button on open

    **footer.tsx** (server component, per D-04, D-05):
    - Import `Link` from `next/link`, `DisclaimerBar` from `@/components/shared/disclaimer-bar`
    - `<footer className="bg-cream border-t border-stone-200">`
    - Inner: `<div className="max-w-7xl mx-auto px-6 md:px-8 pt-12 pb-8">`
    - Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12">`
    - Column 1 (Brand): `<div>` with `<p className="font-heading text-base font-bold text-charcoal">The Ledger</p>` and `<p className="text-sm text-stone-600 mt-2">Clear accounting guidance for Irish business owners.</p>`
    - Column 2 (Quick Links): heading `<p className="text-sm font-bold text-charcoal uppercase tracking-wider mb-4">EXPLORE</p>`, then `<nav aria-label="Footer navigation"><ul className="flex flex-col gap-2">` with links to /guides, /find-accountant, /about, /contact. Each: `<li><Link className="text-sm text-stone-600 hover:text-burnt-orange-500 transition-colors duration-150">`
    - Column 3 (Legal): heading "LEGAL", links to /privacy ("Privacy Policy") and /terms ("Terms of Service"), same styling
    - Below grid: `<div className="border-t border-stone-200 pt-6 mt-8">` containing the disclaimer text directly (same text as DisclaimerBar: "The information on this site is for educational purposes only..."), styled as `<p className="text-sm text-stone-500 max-w-3xl">`
    - Copyright: `<p className="text-sm text-stone-500 mt-6">&copy; 2026 The Ledger. All rights reserved.</p>`
    - Note: Rather than importing DisclaimerBar (which has its own border-t and mt-12 styling that conflicts with footer layout), inline the disclaimer text directly in the footer with the footer-specific spacing (border-t pt-6 mt-8).
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/layout/mobile-menu.tsx` exists and contains `use client` and `role="dialog"` and `aria-modal="true"` and `Mobile navigation` and `Escape` and `overflow`
    - File `src/components/layout/footer.tsx` exists and contains `Footer navigation` and `EXPLORE` and `LEGAL` and `Privacy Policy` and `Terms of Service` and `2026 The Ledger`
    - Mobile menu has focus trap logic with Tab key handling
    - Mobile menu locks body scroll when open and cleans up on unmount
    - Mobile menu closes on Escape key, X button click, and nav link click
    - Footer renders 3-column grid with `grid-cols-1 md:grid-cols-3`
    - Footer contains the disclaimer text inline
    - Footer contains copyright line with 2026
  </acceptance_criteria>
  <done>Mobile menu overlay renders full-screen with focus trap, Escape close, body scroll lock, and nav links. Footer renders 3-column grid with brand info, quick links, legal links, disclaimer text, and copyright line.</done>
</task>

</tasks>

<verification>
All 5 layout component files exist in src/components/layout/:
- `npx tsc --noEmit` passes (no TypeScript errors)
- skip-to-content.tsx exports SkipToContent targeting #main-content
- header.tsx exports Header with fixed positioning and "The Ledger" wordmark
- navigation.tsx exports client Navigation with usePathname active detection
- mobile-menu.tsx exports client MobileMenu with focus trap and body scroll lock
- footer.tsx exports Footer with 3-column grid and disclaimer
</verification>

<success_criteria>
- All 5 component files compile without TypeScript errors
- Header has sticky positioning (fixed, z-50, h-16, backdrop-blur)
- Navigation detects active links via usePathname
- Mobile menu has focus trap, Escape close, body scroll lock
- Footer has 3-column grid, disclaimer, copyright
- Skip-to-content link targets #main-content
</success_criteria>

<output>
After completion, create `.planning/phases/05-navigation-accessibility/05-01-SUMMARY.md`
</output>
