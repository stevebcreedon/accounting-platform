---
phase: 09-animation-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/motion/motion-provider.tsx
  - src/components/motion/page-transition.tsx
  - src/components/motion/scroll-reveal.tsx
  - src/components/motion/reading-progress-bar.tsx
  - src/app/globals.css
  - src/components/shared/article-card.tsx
  - src/components/shared/category-card.tsx
  - src/components/layout/navigation.tsx
autonomous: true
requirements:
  - ANIM-03
  - ANIM-04
  - ANIM-06
  - ANIM-07

must_haves:
  truths:
    - "LazyMotion provider exists and can be imported by layout"
    - "ScrollReveal component wraps children with fade+slide on viewport entry"
    - "ReadingProgressBar renders a 4px burnt-orange fixed bar tracking scroll"
    - "PageTransition renders an opacity fade-in wrapper"
    - "Article cards scale up and lift on hover via CSS"
    - "Category cards scale up and lift on hover via CSS"
    - "Navigation links show underline slide-in on hover"
    - "All CSS animations are disabled when prefers-reduced-motion is active"
  artifacts:
    - path: "src/components/motion/motion-provider.tsx"
      provides: "LazyMotion wrapper with domAnimation"
      contains: "use client"
    - path: "src/components/motion/page-transition.tsx"
      provides: "Opacity fade page transition"
      contains: "use client"
    - path: "src/components/motion/scroll-reveal.tsx"
      provides: "whileInView scroll reveal wrapper"
      contains: "whileInView"
    - path: "src/components/motion/reading-progress-bar.tsx"
      provides: "Scroll-based progress bar"
      contains: "useScroll"
    - path: "src/app/globals.css"
      provides: "Reduced motion media query and nav-link underline"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/components/motion/motion-provider.tsx"
      to: "motion/react"
      via: "LazyMotion import"
      pattern: "LazyMotion"
    - from: "src/components/motion/scroll-reveal.tsx"
      to: "motion/react-m"
      via: "m component import"
      pattern: "motion/react-m"
---

<objective>
Install the motion library and create the 4 motion components (MotionProvider, PageTransition, ScrollReveal, ReadingProgressBar) plus CSS-only hover micro-interactions and reduced-motion support.

Purpose: Build all animation primitives and CSS polish so Plan 02 can wire them into the layout and pages.
Output: 4 new client components in src/components/motion/, CSS hover effects on cards/nav, prefers-reduced-motion global rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-animation-performance/09-CONTEXT.md
@.planning/phases/09-animation-performance/09-RESEARCH.md
@.planning/phases/09-animation-performance/09-UI-SPEC.md

@src/app/globals.css
@src/components/shared/article-card.tsx
@src/components/shared/category-card.tsx
@src/components/layout/navigation.tsx

<interfaces>
<!-- Existing patterns the executor needs -->

From src/components/shared/article-card.tsx:
```typescript
// Current className on root Link element:
"block bg-white rounded-xl shadow-card hover:shadow-card-hover transition-shadow duration-200 overflow-hidden"
```

From src/components/shared/category-card.tsx:
```typescript
// Current className on root Link element:
"bg-white rounded-xl border border-stone-200 p-4 text-center hover:bg-burnt-orange-50 hover:border-burnt-orange-200 transition-colors duration-150"
```

From src/components/layout/navigation.tsx:
```typescript
// Current nav link classes (inactive state):
"text-sm font-bold transition-colors duration-150 py-1"
"text-stone-600 hover:text-burnt-orange-500"
// Active state:
"text-charcoal border-b-2 border-burnt-orange-500"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion and create 4 motion components</name>
  <files>
    package.json,
    src/components/motion/motion-provider.tsx,
    src/components/motion/page-transition.tsx,
    src/components/motion/scroll-reveal.tsx,
    src/components/motion/reading-progress-bar.tsx
  </files>
  <read_first>
    package.json,
    .planning/phases/09-animation-performance/09-RESEARCH.md,
    .planning/phases/09-animation-performance/09-UI-SPEC.md
  </read_first>
  <action>
1. Run `npm install motion@^12.38.0`

2. Create `src/components/motion/motion-provider.tsx` (per D-08):
   - "use client" directive
   - Import `LazyMotion` from `motion/react`
   - Dynamic import: `const loadFeatures = () => import("motion/dom-animation").then(mod => mod.default)`
   - If `import("motion/dom-animation")` fails at build time, use fallback pattern: create `src/lib/motion-features.ts` that re-exports `domAnimation` from `motion/react`, then dynamic import that file instead
   - Export `MotionProvider` component wrapping children in `<LazyMotion features={loadFeatures} strict>`

3. Create `src/components/motion/page-transition.tsx` (per D-04):
   - "use client" directive
   - Import `* as m` from `motion/react-m` and `useReducedMotion` from `motion/react`
   - Export `PageTransition` component:
     - `initial={{ opacity: 0 }}`
     - `animate={{ opacity: 1 }}`
     - `transition={{ duration: prefersReducedMotion ? 0 : 0.25, ease: "easeOut" }}`
   - Children passed through

4. Create `src/components/motion/scroll-reveal.tsx` (per D-01):
   - "use client" directive
   - Import `* as m` from `motion/react-m` and `useReducedMotion` from `motion/react`
   - Props: `children: React.ReactNode`, `delay?: number` (default 0), `className?: string`
   - `initial={{ opacity: 0, y: prefersReducedMotion ? 0 : 16 }}`
   - `whileInView={{ opacity: 1, y: 0 }}`
   - `viewport={{ once: true, amount: 0.2 }}`
   - `transition={{ duration: prefersReducedMotion ? 0.2 : 0.4, ease: "easeOut", delay }}`

5. Create `src/components/motion/reading-progress-bar.tsx` (per D-10):
   - "use client" directive
   - Import `useScroll`, `useTransform`, `useReducedMotion` from `motion/react` and `* as m` from `motion/react-m`
   - `useScroll()` for `scrollYProgress`
   - `useTransform(scrollYProgress, [0, 0.02], [0, 1])` for opacity (fade in after scroll starts)
   - If `prefersReducedMotion`, set opacity to 1 always (informational, not decorative)
   - Render `m.div` with:
     - `role="progressbar"` aria-label="Reading progress" aria-valuemin={0} aria-valuemax={100}
     - `className="fixed top-16 left-0 right-0 z-[49] h-1 bg-burnt-orange-500 origin-left"`
     - `style={{ scaleX: scrollYProgress, opacity }}`
   - Height is 4px (h-1 in Tailwind)
   - burnt-orange-500 is #E8720C
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/motion` directory exists
    - `src/components/motion/motion-provider.tsx` contains "use client" and "LazyMotion"
    - `src/components/motion/page-transition.tsx` contains "use client" and "motion/react-m"
    - `src/components/motion/scroll-reveal.tsx` contains "whileInView" and "viewport"
    - `src/components/motion/reading-progress-bar.tsx` contains "useScroll" and "scrollYProgress" and "progressbar"
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 4 motion components exist with correct APIs, motion library installed, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS hover micro-interactions and reduced-motion support</name>
  <files>
    src/app/globals.css,
    src/components/shared/article-card.tsx,
    src/components/shared/category-card.tsx,
    src/components/layout/navigation.tsx
  </files>
  <read_first>
    src/app/globals.css,
    src/components/shared/article-card.tsx,
    src/components/shared/category-card.tsx,
    src/components/layout/navigation.tsx,
    .planning/phases/09-animation-performance/09-UI-SPEC.md
  </read_first>
  <action>
1. Update `src/components/shared/article-card.tsx` (per ANIM-03, D-01):
   - Change root Link className from:
     `"block bg-white rounded-xl shadow-card hover:shadow-card-hover transition-shadow duration-200 overflow-hidden"`
   - To:
     `"block bg-white rounded-xl shadow-card hover:shadow-card-hover hover:scale-[1.02] hover:-translate-y-1 transition-all duration-200 overflow-hidden"`
   - This adds scale and lift on hover. The `transition-all` replaces `transition-shadow` to cover transform too.

2. Update `src/components/shared/category-card.tsx` (per ANIM-03):
   - Change root Link className from:
     `"bg-white rounded-xl border border-stone-200 p-4 text-center hover:bg-burnt-orange-50 hover:border-burnt-orange-200 transition-colors duration-150"`
   - To:
     `"bg-white rounded-xl border border-stone-200 p-4 text-center hover:bg-burnt-orange-50 hover:border-burnt-orange-200 hover:scale-[1.02] hover:-translate-y-1 transition-all duration-200 overflow-hidden"`
   - Changes transition-colors to transition-all, duration 150->200, adds scale + lift.

3. Update `src/components/layout/navigation.tsx` (per ANIM-03):
   - Add `nav-link` class to each desktop nav Link element (inactive state only, not active):
     Change inactive classes from:
     `'text-stone-600 hover:text-burnt-orange-500'`
     To:
     `'text-stone-600 hover:text-burnt-orange-500 nav-link'`
   - The `nav-link` CSS class provides the underline slide-in effect.

4. Add to `src/app/globals.css` the following blocks:

   In `@layer base` section, add at the end (before closing brace):
   ```css
   /* Navigation link hover underline */
   .nav-link {
     position: relative;
   }
   .nav-link::after {
     content: '';
     position: absolute;
     bottom: -2px;
     left: 0;
     width: 100%;
     height: 2px;
     background-color: #E8720C;
     transform: scaleX(0);
     transform-origin: left;
     transition: transform 200ms ease-out;
   }
   .nav-link:hover::after {
     transform: scaleX(1);
   }
   ```

   After the `@media print` block (at the very end of the file), add:
   ```css
   /* Reduced motion support (ANIM-07) */
   @media (prefers-reduced-motion: reduce) {
     *, *::before, *::after {
       animation-duration: 0.01s !important;
       transition-duration: 0.01s !important;
     }
   }
   ```

5. Do NOT modify faq-section.tsx -- FAQ accordion keeps CSS grid-rows animation as-is (per D-02, ANIM-06).
  </action>
  <verify>
    <automated>cd /Users/drsteveclinic/Projects/accounting && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/shared/article-card.tsx` contains "hover:scale-[1.02]" and "hover:-translate-y-1" and "transition-all"
    - `src/components/shared/category-card.tsx` contains "hover:scale-[1.02]" and "hover:-translate-y-1" and "transition-all"
    - `src/components/layout/navigation.tsx` contains "nav-link"
    - `src/app/globals.css` contains "prefers-reduced-motion" and "nav-link::after" and "scaleX(0)"
    - `src/components/article/faq-section.tsx` is NOT modified (ANIM-06 per D-02)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Cards have hover scale+lift, nav links have underline slide-in, reduced-motion media query active, FAQ accordion unchanged, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- All 4 files exist in src/components/motion/ with "use client" directive
- article-card.tsx and category-card.tsx have hover:scale-[1.02] and hover:-translate-y-1
- navigation.tsx uses nav-link class for underline effect
- globals.css has prefers-reduced-motion rule and nav-link CSS
- faq-section.tsx is unmodified
</verification>

<success_criteria>
- Motion library installed and 4 components created (not yet wired into pages)
- CSS hover micro-interactions on cards and nav links
- Reduced-motion support via global CSS rule
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-animation-performance/09-01-SUMMARY.md`
</output>
